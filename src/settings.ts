/** Platform alias users put in config.json. Must match config.schema.json pluginAlias. */
export const PLATFORM_NAME = 'ActronAirNeo'

/** Must match the name field in package.json. */
export const PLUGIN_NAME = 'homebridge-actronair-neo'
